import {
	signup,
	login,
	logout,
	getUser,
	getUsersOfGroup,
	updateUser,
} from "../src/functions/index";

describe("Your test suite", () => {
	beforeAll(() => {
		// Call the prepareFixtures function before running tests
		// prepareFixtures();
	});

	it("should test something", () => {
		// Your test code here
	});

	// More test cases...
});
